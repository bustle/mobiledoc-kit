import { formatObject } from '../../../helpers/format-object';
import { module, test } from 'qunit';

module('Unit | Helper | format object');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatObject([{}]);
  assert.ok(result);
});
